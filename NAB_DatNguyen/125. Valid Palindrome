/**
 * @param {string} s
 * @return {boolean}
 */

//Regex + reverse method
var isPalindrome = function (s) {
    s = s.toLowerCase().replace(/[^a-z0-9]/g, "");
    let reversed = s.split("").reverse().join("");
    return s === reversed;
};

//Two pointer methods
//Custome an isAlphaNumeric() function to check if a character is number or letter
// abba is an Palindrome 
// => Using 2 pointer moving from head and tail of a string to check if that string is palindrome
// => If any of pair character is not-matched => return false
// else return true
/**
 * @param {string} s
 * @return {boolean}
 */
var isPalindrome = function (s) {
  let left = 0;
  let right = s.length - 1;

  const isAlphaNumeric = (ch) => {
    return /^[a-z0-9A-Z]$/i.test(ch);
  };

  while (left < right) {
    while (left < right && !isAlphaNumeric(s[left])) {
      left++;
    }
    while (left < right && !isAlphaNumeric(s[right])) {
      right--;
    }

    if (s[left].toLowerCase() !== s[right].toLowerCase()) {
      return false;
    }

    left++;
    right--;
  }

  return true;
};
